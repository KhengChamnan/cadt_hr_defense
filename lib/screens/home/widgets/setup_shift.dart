import 'package:flutter/material.dart';

class SetupShift extends StatelessWidget {
  const SetupShift({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Setup Shift Is not implemented yet');
  }
}